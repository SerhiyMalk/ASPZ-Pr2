# ASPZ-Pr2

# Завдання 2.1
<img width="844" height="142" alt="image" src="https://github.com/user-attachments/assets/8be25c7e-24f8-4cc0-9071-1bb1a217e25e" />

# Завдання 2.2
Базова програма: Якщо скомпілювати звичайний "hello world" і запустити size a.out, ми побачимо базові розміри сегментів.
Глобальний неініціалізований масив: Якщо додати int arr[1000]; розмір файлу a.out на диску майже не зміниться, але сегмент bss збільшиться на 4000 байт (1000 * 4 байти).
Глобальний ініціалізований масив: Якщо змінити оголошення на int arr[1000] = {1};, масив переміститься з bss у сегмент data. Це призведе до збільшення розміру сегмента data і фізичного розміру файлу a.out, оскільки ці дані зберігаються у виконуваному файлі.
Локальні масиви: Якщо оголосити ці масиви всередині main(), розміри data та bss не зміняться. Локальні змінні створюються в пам'яті під час виконання програми (у стеку) і не зберігаються в самому a.out.
Оптимізація та налагодження: Компіляція з прапорцем -g  збільшує загальний розмір файлу через додавання символів налагодження, але не впливає на базові сегменти. Використання прапорців оптимізації в першу чергу вплине на зменшення текстового сегмента (text), де зберігаються інструкції.
# Завдання 2.3
<img width="645" height="272" alt="image" src="https://github.com/user-attachments/assets/73f62244-a426-4e47-abdf-226d3729fdb9" />

# Завдання 2.4
<img width="815" height="329" alt="image" src="https://github.com/user-attachments/assets/375ad2c7-59d6-4c21-8d37-391738ff5bf1" />

# Завдання 2.5
У класичній фон-нейманівській архітектурі обійтися без IP неможливо.
Лічильник команд (Instruction Pointer, IP/PC) вказує процесору, де знаходиться наступна інструкція в текстовому сегменті для виконання. Стек же використовується виключно для збереження контексту: локальних змінних, параметрів функцій та адрес повернення.

### Завдання 1: Визначення моменту переповнення `time_t`
* [cite_start]**Опис:** Програма для визначення моменту, коли закінчиться `time_t`[cite: 69, 70].
* [cite_start]**Результат:** Досліджено відмінності у поведінці цього типу даних на 32- та 64-бітних архітектурах (Проблема 2038 року)[cite: 70].

### Завдання 2.2: Дослідження сегментів виконуваного файлу
* [cite_start]**Опис:** Аналіз сегментів у виконуваному файлі за допомогою утиліти `size`[cite: 72, 73, 74].
* [cite_start]**Результат:** Практично перевірено вплив глобальних та локальних масивів (ініціалізованих та неініціалізованих) на розміри сегментів `data` та `BSS`[cite: 75, 76, 77, 78]. [cite_start]Проаналізовано, як на розмір файлу `a.out` впливає компіляція для налагодження[cite: 80, 85].

### Завдання 2.3: Розташування сегментів у пам'яті
* [cite_start]**Опис:** Визначення приблизного розташування стека, сегментів даних, тексту та купи всередині адресного простору процесу[cite: 86, 87, 94].
* [cite_start]**Результат:** Виведено адреси змінних, поміщених у різні сегменти[cite: 94]. [cite_start]Продемонстровано, що на більшості процесорів стек зростає вниз, до пам’яті з меншими значеннями адрес, що підтверджено викликом функцій та оголошенням локальних масивів[cite: 95, 97].

### Завдання 2.4: Дослідження стека процесу (GDB)
* [cite_start]**Опис:** Дослідження стека процесу на прикладі програми, що виконує вкладені виклики функцій та переходить у блокуючий сплячий режим через виклик `pause()`[cite: 98, 99, 120, 122].
* [cite_start]**Результат:** Виконано приєднання до працюючого процесу вручну за допомогою налагоджувача `GDB`[cite: 101, 119]. [cite_start]За допомогою команди `backtrace` (або `bt`) проаналізовано стек викликів[cite: 153].

### Завдання 2.5: Лічильник команд (IP) та вершина стека
* [cite_start]**Опис:** Аналіз питання щодо того, чи можна обійтися без лічильника команд (IP), використовуючи замість нього вершину стека при виклику процедур і поверненні з них[cite: 172, 173].
* [cite_start]**Результат:** Дано обґрунтовану відповідь, яка підтверджує базові принципи роботи процесора та виконання інструкцій[cite: 174].

### Індивідуальне завдання: Перевірка ASLR
* [cite_start]**Опис:** Реалізація програми, що перевіряє випадкове розташування сегментів (ASLR)[cite: 175, 186].
* **Результат:** Створено утиліту, яка при кожному запуску демонструє зміну адрес для стека, купи та динамічних бібліотек, підтверджуючи роботу механізмів безпеки ОС Linux.

Вершина стека вказує лише на дані в оперативній пам'яті. Навіть коли процесор читає адресу повернення зі стека , він копіює цю адресу назад у лічильник команд, щоб продовжити виконання. Без регістра IP процесор просто не знатиме, яку інструкцію читати наступною.

# Завдання 2.6
<img width="794" height="587" alt="image" src="https://github.com/user-attachments/assets/599b6cc3-6a49-49ed-b99e-8b8018efc776" />
